

для запуску створіть та активуйте віртуальне оточення:
   python -m venv venv
   source venv/Scripts/activate  # Для Windows (Git Bash)
Встановіть залежності:
pip install -r requirements.txt

Запустіть програму:
python main.py